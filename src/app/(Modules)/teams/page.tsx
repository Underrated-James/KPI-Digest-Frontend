import { Metadata } from "next";
//import { TeamPage } from "@/features/teams";

export const metadata: Metadata = {
  title: "Teams | KPI Digest",
  description: "Manage your teams.",
};

// export default function Teams() {
//   return <TeamPage />;
// }

export default function Teams(){

  return(
    <div>
      <h1>Teams</h1>
    </div>
  )
}
